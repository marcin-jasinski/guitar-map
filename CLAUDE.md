## Agent skills

### Issue tracker

Local markdown — issues, specs, and the wayfinder map live as files under `wayfinder/` (authoritative convention: `wayfinder/TRACKER.md`). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root, created lazily. See `docs/agents/domain.md`.
